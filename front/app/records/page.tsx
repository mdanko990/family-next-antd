import DataTable from "../components/table";

export default function Records() {
    return (
        <div>
            Records page
            <DataTable data={[]} columns={[]}/>
        </div>
    );
}
